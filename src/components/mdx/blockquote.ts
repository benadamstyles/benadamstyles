import styled from '@emotion/styled'

import { linkColor } from '../../css/Colors.gen'

const Blockquote = styled.blockquote`
  border-left: 5px solid ${linkColor};
  padding-left: 1rem;
  margin-left: 1rem;
  font-style: italic;
`

export default Blockquote
